# CODSWALLOP

Repository for some experiments with simple games over the web.
Current stack is SvelteKit with static adapter on the frontend (managed with pnpm) and Python with FastAPI on the backend (managed with UV). All of it is supposed to be dockerized.
